services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ${HOME}/data/sqlite:/app/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "8080:80"
  #   restart: unless-stopped
