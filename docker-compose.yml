services:
  backend:
    build: ./backend
    volumes:
      - ${HOME}/data/sqlite
    env_file:
      - ./backend/.env
    restart: unless-stopped

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf
      - ./nginx/certs
    depends_on:
      - backend
    restart: unless-stopped
